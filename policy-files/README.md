# Policy source Word files

Final board-approved DOCX sources used for the website policy pages and PDFs.

A dated publication record (PDFs + page sources + DOCX + live HTML snapshots) is kept under:

`backups/policies-YYYY-MM-DD/`

Current snapshot: `backups/policies-2026-07-28/`
